@model List<Calorie.Models.Trackers.runKeeperVM>

<table class="table table-striped">
    @foreach (var itm in @Model)
    {
        <tr>
            <td>
                @Html.Partial("..//Offsets//runKeeper//_RunKeeperActivityPartial", @itm)
            </td>
        </tr>
    }

</table>
